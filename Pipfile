[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
pytest = "*"

[packages]
aiofiles = ">=0.5.0"
fastapi = ">=0.45<2.0"
fastapi-migrations = ">=0.0.4"
loguru = ">=0.4.0"
uvicorn = ">=0.12"

[requires]
python_version = "3.7"

[scripts]
start = "python app/main.py"
test = "pytest tests -vv --show-capture=all"
